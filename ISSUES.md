# 本地 Issue

## ISSUE-001（GitHub #1）：UP 主管理页签同步会将浏览器切换到前台

- 状态：暂不纳入当前 MVP
- 优先级：中

### 问题

在“UP 主管理”页签执行同步时，浏览器会被切换到前台，打断用户当前工作。

### 期望行为

同步过程应在后台运行，不抢占或切换用户正在使用的浏览器窗口焦点。

### 验收条件

- 从“UP 主管理”页签发起同步后，浏览器窗口不会自动前置或获得焦点。
- 同步仍能正常完成，并在页面显示原有的进度和结果。
- macOS 与 Windows 使用同一套业务实现；系统差异仅允许位于 `core/utils/system/`。

### 处理结论

现有调用已使用 OpenCLI 的 `--window background`。GitHub Issue #1 提出的专用 Chrome、扩展安装和登录引导会新增浏览器生命周期与登录能力，超出当前 MVP；保留 GitHub Issue 跟踪，不在本地实现。

## ISSUE-002（GitHub #2）与 ISSUE-003：下载附件与本地封面命名

- 状态：已解决
- 优先级：中

### 问题

OpenCLI/yt-dlp 下载视频后，封面 JPG 与音频 M4A 仍使用 BV 号前缀，而 MP4 已按知识库规则重命名，文件系统中无法直接按同一主名配对。

### 处理

实际下载分支在 MP4 重命名后，只对同目录、文件名包含同一 BV 号的 JPG/JPEG/WEBP/M4A 附件执行配对命名：图片改为 `<视频主名>_cover.<扩展名>`，M4A 改为 `<视频主名>.m4a`。找不到附件、目标已存在或重命名失败时跳过；不迁移历史文件。

### 验收条件

- 新下载视频生成的封面使用 `<视频主名>_cover.<扩展名>`，音频附件与视频使用同一文件主名。
- 不影响视频主流程、下载状态或历史文件。
- 通过自动化测试覆盖 JPG/M4A 配对命名。

## ISSUE-003：下载视频时同时保存本地封面

- 状态：已由 ISSUE-002 合并解决
- 优先级：中

### 需求

视频下载到本地时，需将对应的视频封面一并保存到同一目录。

### 命名规则

封面使用视频文件的最终主文件名，在扩展名前追加 `_cover`。例如：

```text
视频：标题.mp4
封面：标题_cover.jpg
```

封面也可能使用 PNG 格式，例如 `标题_cover.png`。封面应沿用视频现有的安全命名、去重和目录归档规则；图片扩展名按实际下载格式保留，不强制转换格式。

### 验收条件

- 新下载的视频有对应封面时，视频与封面保存在同一视频目录中。
- 视频重命名后，封面仍以最终视频主文件名加 `_cover` 命名，而不是使用 BV 号命名。
- JPG/JPEG 和 PNG 封面均能正确保存，并保留对应扩展名。
- 封面下载失败或远端没有封面时，不影响视频下载主流程，并留下可诊断的非敏感错误信息。
- 不覆盖已存在的其他视频或封面文件，且不改变历史文件。

### 处理结论

OpenCLI/yt-dlp 在本次下载中生成封面时，应用会保留并按上述 `_cover` 规则命名；未生成封面时不额外发起网络下载，不影响视频主流程。
