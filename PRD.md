# BiliUp MVP 产品需求文档

## 1. 背景与目标

用户需要在本地快速检索 B 站 UP 主，并把确认后的 UP 主信息登记到本项目的 UpList 文件中，供后续视频追踪能力使用。

本 MVP 的目标是完成“搜索 → 选择 → 写入 → 反馈”的单一闭环；应用在 macOS 和 Windows 上以相同功能运行。

## 2. 范围

### 包含

1. 用户输入 UP 主昵称并发起搜索。
2. 后端通过 OpenCLI 搜索 B 站用户。
3. 网页展示候选结果的昵称、UP ID 与简介。
4. 用户一次只能选择一个候选结果并确认写入。
5. 系统创建或更新本地 UpList，并提示结果。
6. 首次运行检测 OpenCLI；未安装或不可运行时展示明确的准备步骤并暂时禁用搜索。

### 不包含

- UP 主视频列表、视频下载、下载状态、字幕、转录、定时追踪、登录、迁移、云同步和文件整理。
- 头像展示、批量选择/批量写入、删除、编辑和排序功能。
- 参考工作流中每位 UP 的视频 `.jsonl` 或 Markdown 视频记录。

## 3. 用户流程

1. 用户打开本地 WebUI，在搜索框输入昵称。
2. 网页在输入停止约 500ms 后调用搜索接口；空白输入不调用接口。
3. 系统显示匹配项的昵称、UP ID 与简介；没有结果时显示“未找到匹配的 UP 主”。
4. 用户单选一项并点击确认写入。
5. 系统按 UP ID 写入或更新 UpList，显示“已新增”或“已更新”；失败时显示简短错误信息，不写入半成品数据。

## 4. 功能需求

### 4.1 搜索

- 搜索输入按去除首尾空白后的昵称处理。
- `POST /api/up-search` 接收昵称，返回 `uid`、`nickname`、`bio`。
- `core` 层通过 OpenCLI 执行用户搜索并标准化结果；网页和 FastAPI 路由不直接执行命令。
- OpenCLI 无结果、超时或返回不可解析数据时，系统返回可理解的失败信息。
- 已安装 OpenCLI 时，打包应用必须在 Finder/Explorer 启动环境中正确定位它；未安装时不得只返回笼统错误。

### 4.2 选择与确认

- 搜索结果只能单选。
- 未选择结果时，确认按钮不可写入。
- 用户确认后，前端发送选定的 `uid`、`nickname`、`bio` 到 `POST /api/followings`。

### 4.3 本地写入

数据根目录为项目内相对路径 `UpList/`。

- `UpList/followings.json` 是唯一真源，为按添加顺序保存的 JSON 数组。
- 每条记录包含：`uid`、`nickname`、`bio`、`scheduled_tracking`、`created_at`、`last_sync_at`。
- `uid` 是唯一键。新 UID 追加记录；已有 UID 仅更新昵称和简介，保留原 `created_at`。
- 空简介保存为 `-`。新记录的 `scheduled_tracking` 为 `true`；`created_at` 与 `last_sync_at` 使用当前带时区的 ISO-8601 时间，以兼容参考格式。
- 写入使用独占锁与原子替换，确保失败不会破坏已有 JSON。
- 写入 JSON 成功后，重建 `UpList/bilibili-up-followings.md`：表头固定为“序号、昵称、UP ID、简介”，序号从 1 连续递增。Markdown 只是派生视图，不能作为写入源。

## 5. 非功能需求

- 三层调用方向固定为：静态网页层 → FastAPI 路由层 → `core` 业务内核层。
- 所有文件操作使用 `pathlib` 与项目相对路径。
- Windows/macOS 差异仅在 `core/utils/system/` 中处理；核心功能与测试代码必须共用。
- 服务仅监听 `127.0.0.1` 或 `localhost`。
- 日志不得记录凭据、Cookie、令牌或完整 OpenCLI 原始输出。

## 6. 验收标准

1. 输入昵称后，用户能看到包含昵称、UP ID、简介的搜索结果或无结果提示。
2. 不选择结果时不能写入；选择一项并确认后产生明确成功或失败反馈。
3. 首次写入创建正确的 JSON 记录和 Markdown 表；相同 UID 再次写入不会产生重复记录。
4. `followings.json` 写入失败时，原文件仍可读取且内容未损坏。
5. macOS 与 Windows 均通过相同自动化测试；涉及启动或打包的改动在两个系统分别验证。
6. 未安装 OpenCLI 时首页显示安装、Chrome 扩展和 B 站登录指引；安装完成后无需修改业务代码即可被识别。

## 7. 参考边界

参考工作流说明了后续“UP 主视频追踪”如何以 UpList 为输入。本 PRD 仅采用其 UP 主信息保存需求，不采用其中旧版 Markdown 视频记录、视频下载、转录或调度流程。
